################################################################################
# SmartIES Stack or Compose
################################################################################
# 
# 
################################################################################
version: "3.7"

services:
  node-red:
    build: https://raw.githubusercontent.com/vilaj/IES/main/stacks/SmartIES/Dokerfile-nodered
    image: nodered:smartIES
    restart: unless-stopped
    environment:
      - TZ=Europe/Amsterdam
    ports:
      - 31880:1880
    networks:
      - net
    volumes:
      - vol:/data
  grafana:    
    image: grafana/grafana:latest
    ports:
      - 33000:3000
    networks:
      - net
    volumes:
      - vol:/data

volumes:
  vol:

networks:
  net:
